# Session Handoff - November 26, 2025

## Session Summary

This session completed two main tasks:

### 1. Azure Engagement Tracking Disabled (COMPLETED)

**Problem**: Double URL tracking causing spam issues - both listmonk's native `{{ TrackLink }}` and Azure Communication Services User Engagement Tracking were wrapping URLs, creating excessively long links.

**Solution**: Created and ran `/tmp/disable_tracking.sh` to disable Azure engagement tracking across all domains.

**Results**:
- 31 email services processed
- 88 domains had tracking disabled
- 30 domains were already disabled (frigid-email-service)
- 0 failures

### 2. Bypass Sending Time Window Feature (COMPLETED)

**Problem**: Cannot test queue-based campaigns outside configured sending time window (e.g., after 8pm).

**Solution**: Added per-campaign `bypass_time_window` boolean flag.

**Files Modified**:
- `models/models.go` - Added BypassTimeWindow field to Campaign struct
- `internal/migrations/v7.3.0.go` - NEW: Migration to add column
- `cmd/upgrade.go` - Registered v7.3.0 migration
- `internal/queue/processor.go` - Logic to respect bypass flag
- `queries.sql` - Updated create-campaign ($21) and update-campaign ($20)
- `internal/core/campaigns.go` - Pass new parameter in CreateCampaign/UpdateCampaign
- `frontend/src/views/Campaign.vue` - UI toggle (only for automatic messenger)

**Deployment Required**:
```bash
# Run migration
./listmonk --upgrade

# Or manually
ALTER TABLE campaigns ADD COLUMN IF NOT EXISTS bypass_time_window BOOLEAN NOT NULL DEFAULT FALSE;

# Rebuild frontend
make build-frontend
```

## Uncommitted Changes

Run `git status` to see all changes. Key files:
- `models/models.go`
- `internal/migrations/v7.3.0.go` (new)
- `cmd/upgrade.go`
- `internal/queue/processor.go`
- `queries.sql`
- `internal/core/campaigns.go`
- `frontend/src/views/Campaign.vue`

## Background Task

Background task d83bc4 (Azure tracking disable script) completed successfully.

## Next Steps If Continuing

1. Test the bypass time window feature in browser
2. Consider adding i18n translations for the new UI label
3. Commit the changes with descriptive message
4. Deploy to production and run migration

## Build Status

Backend builds successfully:
```bash
go build -o /dev/null cmd/*.go  # No errors
```

Frontend not tested locally (no local DB connection available).
